# Earlier Streamlit interface

This is the original interface prototype, previously stored in `Interface and Intergration/`. It includes a live-monitor page, sample sessions, and an archive browser. Its adapters use simulated data and heuristics.

The main integrated application is now [`app/`](../../app/). See the [local setup guide](../../docs/development/running-locally.md) to run either version.
